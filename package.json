{
	"private": true,
	"type": "module",
	"scripts": {
		"build": "npm run build:script && ASTRO_TELEMETRY_DISABLED=1 astro build",
		"build:script": "tsc -p tsconfig.script.json",
		"dev": "npm run build:script && ASTRO_TELEMETRY_DISABLED=1 astro dev",
		"preview": "ASTRO_TELEMETRY_DISABLED=1 astro preview",
		"typecheck": "npm run build:script && ASTRO_TELEMETRY_DISABLED=1 astro check"
	},
	"devDependencies": {
		"@astrojs/check": "^0.9.4",
		"astro": "^7.0.2",
		"typescript": "^5.5.0"
	}
}
